% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/matrix_helpers.R
\name{resolve_kinship_input}
\alias{resolve_kinship_input}
\title{Resolve a kinship matrix from either a pedigree upload or a precomputed
relationship matrix upload (A/G/H) supplied by the user.}
\usage{
resolve_kinship_input(
  source,
  pedigree_file = NULL,
  matrix_file = NULL,
  matrix_is_kinship = FALSE
)
}
\arguments{
\item{source}{"pedigree" (default) or "upload"}

\item{pedigree_file}{Shiny file input for the pedigree (used when source == "pedigree")}

\item{matrix_file}{Shiny file input for a precomputed matrix (used when source == "upload")}

\item{matrix_is_kinship}{If TRUE, matrix_file values are treated as already on the
kinship (0.5) scale. If FALSE (default), they are treated as a relationship
matrix (A/G/H) and halved via matrix_to_kinship().}
}
\value{
list(kinship_matrix, pedigree, stats, raw_ped)
}
\description{
Centralizes the logic shared by the Kinship/EBV preview and the Run OCS
handler in the Mate Allocation module, so both stay in sync regardless of
which relationship matrix source the user chooses.
}
